function stringSelect(pw) {
  var sh = pw.length/2;
  return pw.split('').sort().splice(sh, sh);
}

function mapBuild(pw, cc){
  var tmp = pwShadow(pw);
  var pwArr = pw.split('');
  var compass = [];
  var cL = compass.join('').length;
  
  
  var map = pwArr.map(function(val){
       
      if(cc.indexOf(val) > -1){
        compass.push(val);
        return pwArr.indexOf(val);
      } else {
        compass.push(pwArr.indexOf(val));
        return val;
      }
  });
  
  console.log('--BREAK--');
  console.log(pw.split(''));
  console.log(tmp);
  console.log(compass);
  console.log(map);
  
  console.log(stringSelect(password));
  
}

var password = "New Yourk Yankee";

mapBuild(password, stringSelect(password));
